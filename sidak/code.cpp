#include<iostream>
using namespace std;
int main ()
{int nlines,curline,nspace,cval,val,curspace,ninc,ndec;
cin>>nlines;
while(curline<=nlines)
{

	nspace=nlines-curline;
	curspace=1;
	while(curspace<=nspace)
	{cout<<"space";
	curspace=curspace=1;}
}
ninc=ninc+1;
ndec=ndec-1;
val=curline;
while(cval<ninc)
cout<<"val";
val=val+1;
cval=cval+1;
}
val=val-1;
cval=0;
while(cval<=ndec)
{cout<<"val";
val=val-1;
cval=cval+1;
}
cout<<"enter the number";
cin>>number;
curline=curline+1
 }