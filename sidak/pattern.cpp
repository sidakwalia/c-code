#include<iostream>
using namespace std;
int main ()
{ int nline; int nval=0; 
  cin>>nline; //2
  int curline;
  curline=1;
  while(curline<=nline)
  { 
  	if (curline%2==1)
  		{ 
        val=1;
  		}
  		else{ val=0;
  		}
  		int curval=0;
  		while(curval<nval)
  		{cout<<val;
  		 val=1-val;
  		 curval++;
  		}  #include<iostream>
using namespace std;
int main ()
{ int nline; 
  cin>>nline; //2
  int curline;
  curline=1;
  while(curline<=nline)
  { int nvalue<=nline
    if (curline%2==1
      { val=1
      }
      else
      { val=1} 
      int curval=0;
      while(curval<nvalue)
      {cout<<val;
       val=1-val;
       curval++;
      }  
         cout endl;
         curline++;
     }
 }
}
         cout endl;
         curline++;
     }
 }
}